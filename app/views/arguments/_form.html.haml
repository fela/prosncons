%h2 #{@argument.page.title}: #{params[:option]}
= simple_form_for @argument,  update_or_create_path(@argument) do |f|
  = f.error_notification

  .form-inputs
    = f.input :summary
    = f.input :description

  .form-actions
    = f.button :submit


= link_to 'Back', page_path(@argument.page)
