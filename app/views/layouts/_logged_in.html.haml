%li.dropdown
  %a.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
    logged in as
    %strong= session[:email]
    %b.caret
  %ul.dropdown-menu
    %li= link_to "View Profile", user_path(session[:id])
    %li#logout= link_to "Log Out", '#'