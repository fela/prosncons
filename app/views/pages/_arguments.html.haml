.span5
  %h2= option
  - @page.arguments_for(option).each do |a|
    %hr
    %h3= a.summary
    :markdown
      #{a.description}

  - unless @page.arguments_for(option).empty?
    %hr
  =link_to 'Add argument', new_argument_path(@page, option), class: 'btn'