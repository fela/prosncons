.signature
  by #{link_to_user obj.author}, on #{obj.created_at.to_s(:short)}
- edits = obj.versions.size
- if edits > 0
  .edits
    - pluralized = edits == 1 ? "edit" : "edits"
    #{edits} #{link_to pluralized, versions_path(obj)}
